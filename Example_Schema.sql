-- A schema is a method to categorize and compartmentalize stored procedures.  It also provides a method for managing permissions and restrictions.

/* Create a schema named Academy_HR and assign authorization to the database owner (dbo) */
CREATE SCHEMA [Academy_HR] AUTHORIZATION [dbo]

-- Schemas can be found within the Security folder of a database.